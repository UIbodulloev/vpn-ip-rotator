"""Справка внутри бота: что это, какие ключи нужны и откуда их брать.

Вынесено отдельно, чтобы тексты правились без риска задеть логику.
Каждая страница должна укладываться в лимит Telegram (4096 символов).
"""

from __future__ import annotations

OVERVIEW = """📖 Как это работает

Ваш VPN-сервер стоит за границей. Если ТСПУ заблокирует его адрес, сам сервер
об этом не узнает — снаружи России он остаётся доступен. Поэтому наблюдатель
(этот агент) стоит на российском сервере: он раз в минуту проверяет, отвечает
ли VPN, и видит блокировку вашими глазами.

Заметив блокировку, агент через API заказывает серверу новый адрес и
переписывает DNS-запись вашего домена. Клиентские конфиги при этом не меняются —
они указывают на домен, а не на адрес. Людям ничего пересылать не нужно.

Важно, что агент различает четыре разные причины «VPN не работает» и меняет
адрес только в одном случае из четырёх. В остальных смена адреса бесполезна и
только жжёт адреса впустую — тогда приходит алерт с объяснением.

Telegram недоступен с российского хостинга, поэтому агент ходит наружу через
маленький прокси на Cloudflare — релей. Он уже развёрнут, иначе вы бы этого
сообщения не читали.

Дальше: /guide_modes — режимы смены адреса
        /guide_keys — какие ключи нужны и зачем"""

MODES = """🔀 Три способа сменить адрес

1️⃣ Копия здесь — по умолчанию
Снимается шаблон диска, из него поднимается новый сервер в той же локации,
старый удаляется. Даунтайм 5–10 минут. Новый адрес бесплатный, а неиспользованное
время старого сервера возвращается на счёт.

2️⃣ Переезд в другую страну
То же самое, но новый сервер поднимается в другой локации. Копирование диска
между зонами долгое — до получаса. Тоже бесплатно.
Нужен, когда заблокирован не адрес, а вся подсеть провайдера.

3️⃣ Плавающий адрес
Дополнительный адрес поднимается на живом сервере через SSH, разрыва нет.
Стоит денег помесячно и требует SSH-ключа. Если заблокирован и 22-й порт,
режим не сработает — тогда только первые два.

В любом из трёх конфиги клиентов НЕ меняются: ключи уезжают внутри шаблона
диска, а клиенты ходят по домену, а не по адресу.

Почему нет «просто поменять IP»
Такой режим был и был убран. UpCloud не отдаёт свежий адрес на существующий
интерфейс без особых прав, а выданный иначе адрес приезжает на новый интерфейс,
который система на сервере не поднимает. После освобождения старого адреса
сервер остался бы вообще без публичного IP — без VPN и без SSH.

▪️ auto — лестница (рекомендуется)
1. Первая блокировка → копия здесь.
2. Новый адрес умер меньше чем за 6 часов → значит дело в подсети → переезд
   в другую страну.
3. Заблокировало и после переезда → бот останавливается и зовёт вас: менять
   адреса дальше бессмысленно, вопрос в протоколе.

Переключить: /mode auto"""

KEYS = """🔑 Какие ключи нужны

ОБЯЗАТЕЛЬНЫЕ — без них ротация не работает:

1️⃣ Токен UpCloud
Чтобы останавливать сервер, заказывать адреса и создавать серверы.

2️⃣ Сервер
Какой именно сервер ротировать. Выбирается кнопкой из списка — печатать UUID
руками не нужно.

3️⃣ Токен Cloudflare
Чтобы переписывать DNS-запись после смены адреса.

4️⃣ Домен и DNS-запись
Какую запись переписывать. Тоже выбирается кнопкой из списка ваших записей.

НЕОБЯЗАТЕЛЬНЫЕ — каждый включает отдельную возможность:

▫️ Админ-токен UpCloud
Нужен, только если основной токен выпущен под субаккаунтом. После режима
recreate появляется новый сервер, которого в правах субаккаунта нет — админ-токен
позволяет агенту выдать права самому. Без него после переезда придётся один раз
зайти в панель руками.

▫️ Логин субаккаунта
Идёт в паре с админ-токеном — кому выдавать права.

▫️ AAAA-запись
Чтобы вместе с IPv4 обновлялся и IPv6. У UpCloud IPv6 бесплатный, и ТСПУ
фильтрует его заметно реже — это бесплатный второй маршрут.

▫️ SSH-ключ агента (приватный)
Нужен ручному управлению — посмотреть и перезапустить контейнеры, проверить
протоколы — и режиму «плавающий адрес». Смене адреса не нужен: там всё через API.

Разумно держать на VPN-сервере ДВА ключа:
  ваш личный — которым вы ходите на сервер с компьютера;
  ключ агента — которым ходит бот с российского сервера.
Тогда компрометация российского сервера не даёт доступа вашим личным ключом, и
ключ агента можно отозвать одной строкой, не трогая свой. Один ключ на оба
случая тоже работает — просто менее аккуратно.

Задать можно тремя способами (/setup → Необязательное → SSH-ключ агента):
  🔑 сгенерировать пару прямо на российском сервере — приватная часть никуда
     не уезжает, бот покажет публичную и команду, которой её разрешить;
  📋 прислать приватный ключ текстом в чат — бот сохранит его с правами 0600 и
     удалит сообщение; ключ при этом пройдёт через Telegram и Cloudflare;
  📂 указать путь к уже лежащему на сервере файлу.
Публичная часть выводится из приватной автоматически — присылать её отдельно
не нужно.

▫️ SSH-ключ (публичный)
Прописывается новому серверу при копии и переезде.
Важно: в UpCloud НЕТ общего хранилища ключей на уровне аккаунта — ключ
передаётся строкой в момент создания сервера. Выбрать «ключ из панели» нельзя,
такого понятия в API нет.
Если ключ не задан, бот пробует взять файл рядом с приватным (<путь>.pub), а в
мастере есть /auto. Если не нашлось ничего — сервер всё равно создастся, и
доступ сохранится тем ключом, что запечён в шаблоне диска (он клонируется
вместе с /root/.ssh/authorized_keys). Но полагаться на это молча не стоит:
/check предупредит.

Где всё это взять: /guide_where
Заполнить: /setup"""

CHATS = """💬 Личный чат или группа

Настраивать бота проще всего в ЛИЧНОМ чате. Причина техническая.

У ботов Telegram по умолчанию включён режим приватности: в группах бот получает
только команды (/check, /setup) и ответы на свои сообщения. Обычный текст ему не
доставляется вообще — ни строчки в журнале. Выглядит это ровно так: команды
работают, а присланный токен «пропадает».

Если всё же нужна группа, приватность отключается так:
@BotFather → /setprivacy → выберите бота → Disable.
После этого ОБЯЗАТЕЛЬНО удалите бота из группы и добавьте заново — на уже
добавленного бота настройка не подействует.

Разрешённых чатов может быть несколько: в TG_ADMIN_ID пишутся ID через запятую.
Удобно держать личный чат для настройки и группу для уведомлений.

Если написать боту из чата, которого нет в списке, он пришлёт в основной чат
предложение разрешить этот — с кнопкой и готовым ID. Ничего искать не нужно.

Свой ID можно узнать у @userinfobot, ID группы — тоже, если добавить его туда."""

WHERE = """🧭 Где взять токены

▪️ Токен UpCloud
Панель UpCloud → People (или Account) → API tokens → Create token.
Токен начинается с ucat_.
Безопаснее выпустить его под субаккаунтом, у которого права только на ваш
VPN-сервер: People → Add user, затем Permissions → target: server → ваш сервер.
При создании токена можно ограничить allowed IP ranges — впишите туда адрес
этого российского сервера.

▪️ Токен Cloudflare
dash.cloudflare.com → My Profile → API Tokens → Create Token
→ Create Custom Token.
Права: Zone → DNS → Edit (и всё, больше ничего не нужно).
Zone Resources: Include → Specific zone → ваш домен.
НЕ используйте Global API Key — он даёт доступ ко всему аккаунту.

▪️ Админ-токен UpCloud (необязательный)
Тот же путь, но под основным аккаунтом, а не субаккаунтом. Обязательно
ограничьте его по allowed IP ranges.

▪️ SSH-ключ (необязательный)
На этом сервере: ssh-keygen -t ed25519 -f /etc/vpn-rotator/id_vpn -N ""
Публичную часть (/etc/vpn-rotator/id_vpn.pub) добавьте в authorized_keys на
VPN-сервере, приватную (/etc/vpn-rotator/id_vpn) укажите боту как путь.

Заполнить: /setup"""

CHECKLIST = """✅ Что проверить до боевого режима

Пока в конфиге стоит dry_run, ротации только имитируются. Снимать его стоит
после четырёх проверок.

1. /check — все доступы зелёные.

2. Клиенты ходят по домену, а не по адресу.
   Это главное: если в конфигах прописан IP, смена DNS ничего не переключит.
   В AmneziaWG: Endpoint = ваш.домен:порт
   В VLESS-ссылке замените хост на домен, SNI не трогайте.
   TTL записи в Cloudflare поставьте 60, не Auto.
   Раздайте людям два конфига — по домену и запасной по IP.

3. Детектор вхолостую.
   На этом сервере зарежьте трафик к VPN:
     sudo iptables -A OUTPUT -d АДРЕС -p tcp --dport 443 -j DROP
     sudo iptables -A OUTPUT -d АДРЕС -p tcp --dport 22 -j DROP
   Через ~5 минут должен прийти вердикт «блокировка IP».
   Снимите правила: те же команды с -D вместо -A.

4. Различение — самая важная проверка.
   Зарежьте только порт 443, оставив 22.
   Вердикт обязан быть «блок порта/протокола», и ротация НЕ предлагается.
   Это и защищает от бессмысленного сжигания адресов.

Потом: уберите dry_run в /etc/vpn-rotator/config.toml,
systemctl restart vpn-rotator, и /mode auto."""

TESTING = """🧪 Как проверить по-настоящему

Три уровня проверки, от безопасного к боевому.

1. Ничего не выполняется
/plan — покажет точные шаги каждого режима с ВАШИМИ значениями: какой сервер,
какой адрес, какой диск, сколько продлится, во что обойдётся.
/simulate — прогонит пробы и скажет, что сделала бы автоматика прямо сейчас.
/probe — просто проверит доступность.

2. Репетиция с dry_run
Пока в конфиге dry_run = true, ротация не выполняется, но ведёт себя как
настоящая: пишется в историю, включает cooldown и дневной лимит. Так проверяется
детектор. Зарежьте на этом сервере трафик к VPN:
  sudo iptables -A OUTPUT -d АДРЕС -p tcp --dport 443 -j DROP
  sudo iptables -A OUTPUT -d АДРЕС -p tcp --dport 22 -j DROP
Через ~5 минут придёт вердикт «блокировка IP».
Снимите правила теми же командами с -D вместо -A.
Обязательно повторите, зарезав ТОЛЬКО 443: вердикт должен стать «блок
порта/протокола», и ротация предлагаться не будет.

3. Боевой прогон
Единственный способ убедиться, что режим отработает — выполнить его руками в
удобное время. Порядок от безопасного к серьёзному:
  /rotate → replace-ip — 2-3 минуты, бесплатно, легко повторить;
  /rotate → recreate в свою зону — проверяет шаблон и то, что старые конфиги
  клиентов продолжают работать;
  /rotate → recreate в другую зону — самый долгий, проверяет переезд.
После каждого: /ip покажет, сошлись ли адрес и DNS, а клиенты должны
подключиться БЕЗ правки конфигов.

Совсем без риска для рабочего VPN: создайте в UpCloud копеечный сервер,
переключите на него SERVER_UUID через /setup, прогоните все режимы, потом
верните обратно. Домен на время теста укажите запасной."""

MONEY = """💶 Во что это обходится

replace-ip — бесплатно.
Новый адрес выдаётся на время остановки, старый освобождается. Постоянно у
сервера остаётся один адрес, включённый в план.

Копия и переезд — бесплатно по адресам, но есть шаблон.
Новый сервер получает адрес бесплатно, а тарификация серверов почасовая, так что
за старый вы платите только до момента удаления.

Что тарифицируется ВО ВРЕМЯ переезда, минут десять-сорок:
  старый сервер — остановлен, значит только диск (у Flexible-планов при
  выключении CPU и RAM не считаются, хранилище и адреса считаются);
  шаблон диска — как дополнительное хранилище, по ГБ в час;
  новый сервер — полностью, с момента создания.
То есть недолго вы платите примерно за полтора сервера. На масштабе 10 ГБ и
получаса это копейки.

После переезда остаётся один шаблон как точка отката (/rollback), и он
продолжает тарифицироваться. Не нужен — поставьте keep_templates = 0, тогда
шаблоны удаляются сразу после успешного переезда.

floating — платно.
Плавающий адрес тарифицируется за каждый зарезервированный адрес, пока он
числится за аккаунтом, даже когда не привязан к серверу. Освобождённый адрес
перестаёт тарифицироваться.

IPv6 — бесплатно, по одному адресу на сервер.

Резервных копий UpCloud (Simple/Flexible Backups) этот скрипт НЕ использует и
не включает. Шаблон снимается разово под конкретную ротацию и удаляется по
настройке keep_templates. Платные бэкапы включать не нужно."""

MANUAL = """🖥 Ручное управление сервером

Меню 🖥 делает то же, что панель UpCloud, но из чата. DNS при этом НЕ трогается —
кроме случая, когда вы явно запускаете смену адреса.

▶️ Запустить / ⏸ Остановить
Обычный power on/off. Адрес и диск сохраняются. Пока сервер остановлен, VPN не
работает, но ничего не теряется.

🔁 Перезагрузить
Мягкая остановка и запуск, около минуты. Адрес не меняется. Помогает, если
сервер жив, но ведёт себя странно.

📸 Снять шаблон (дамп)
Сервер останавливается, с диска снимается образ, сервер запускается обратно.
В образе всё: ключи AmneziaWG и Reality, база клиентов, настройки, swap.
Из такого шаблона поднимается новый сервер, и старые конфиги клиентов
продолжают работать. Шаблон тарифицируется как дополнительное хранилище.

🆕 Поднять сервер из шаблона
Создаёт НОВЫЙ сервер рядом со старым, в локации шаблона. Старый не удаляется и
DNS не обновляется — это ручной режим, всё под вашим контролем. Удобно, чтобы
проверить переезд, ничего не ломая.
Если нужен именно переезд целиком — используйте 🔄 Сменить адрес, там всё это
делается одной операцией, включая DNS и удаление старого.

🐳 Контейнеры
Смотрит и перезапускает контейнеры Amnezia по SSH. Нужен приватный SSH-ключ:
/setup → Необязательное. Ротации ключ не нужен.

🔍 Протоколы
Показывает, слушаются ли порты 443 и 35206 на самом сервере, сколько места и
памяти, и как сервер выглядит снаружи — из России.

🗑 Удалить сервер
Удаляет сервер и его диск безвозвратно, адрес освобождается. Делайте это только
после того, как новый сервер поднят и проверен."""

PAGES = {
    "overview": ("📖 Как это работает", OVERVIEW),
    "modes": ("🔀 Режимы", MODES),
    "keys": ("🔑 Ключи", KEYS),
    "where": ("🧭 Где взять", WHERE),
    "chats": ("💬 Личка или группа", CHATS),
    "manual": ("🖥 Ручное управление", MANUAL),
    "testing": ("🧪 Как проверить", TESTING),
    "money": ("💶 Деньги", MONEY),
    "checklist": ("✅ Проверки", CHECKLIST),
}

MENU = """❓ Справка

Выберите раздел кнопкой ниже, или командой:
/guide_overview — как это работает
/guide_modes — режимы смены адреса
/guide_keys — какие ключи нужны
/guide_where — где взять токены
/guide_manual — ручное управление сервером
/guide_testing — как проверить по-настоящему
/guide_money — во что обходится
/guide_chats — личный чат или группа
/guide_checklist — проверки до боевого режима"""
